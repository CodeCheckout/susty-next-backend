name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "b4aa07a6-7971-49cc-8c90-228ca9c21ca1"
          heroku_app_name: "susty-server-backend" #Must be unique in Heroku
          heroku_email: "susty.co.nz@gmail.com"
